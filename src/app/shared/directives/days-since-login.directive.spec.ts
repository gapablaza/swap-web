import { DaysSinceLoginDirective } from './days-since-login.directive';

describe('DaysSinceLoginDirective', () => {
  it('should create an instance', () => {
    const directive = new DaysSinceLoginDirective();
    expect(directive).toBeTruthy();
  });
});
